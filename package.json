{
  "name": "cloudeless-p",
  "license": "MIT",
  "scripts": {
    "terraform": "cd ../; ./tf",
    "sls": "cd ../; ./sls",
    "serveless:deploy": "npm run serverless -- deploy",
    "build": "mkdir -p artifact && cd src && cp bootstrap ../artifact/bootstrap && cp run.sh ../artifact/run.sh && cd ../artifact && chmod +x bootstrap run.sh && zip -m function.zip bootstrap run.sh",
    "deploy:function": "npm run serverless -- deploy function",
    "deploy": "npm run serverless -- deploy",
    "logs:tail": "npm run serverless -- logs --tail"
  },
  "dependencies": {
    "@fastify/aws-lambda": "^5.1.4",
    "@fastify/cors": "^11.0.1",
    "debug": "^4.4.1",
    "fastify": "^5.4.0",
    "http-errors": "^2.0.0",
    "puppeteer": "^24.12.1"
  },
  "devDependencies": {
    "serverless": "^4.17.2"
  }
}
